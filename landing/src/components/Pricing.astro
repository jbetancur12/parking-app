---
import { Check, Zap } from 'lucide-react';

const plans = [
    {
        name: "Emprendedor",
        price: "80.000",
        period: "/mes",
        description: "Perfecto para parqueaderos pequeños que están comenzando",
        features: [
            "1 sede",
            "Hasta 50 cupos",
            "Gestión de entradas y salidas",
            "Clientes mensuales ilimitados",
            "Reportes básicos",
            "Impresión de tickets",
            "Soporte por email"
        ],
        cta: "Comenzar Gratis",
        highlighted: false
    },
    {
        name: "Pro",
        price: "180.000",
        period: "/mes",
        description: "Ideal para parqueaderos establecidos que necesitan más control",
        features: [
            "Todo lo del Emprendedor",
            "Hasta 3 sedes",
            "Cupos ilimitados",
            "✨ Integración facturación electrónica DIAN",
            "Reportes avanzados y exportación",
            "Control de turnos",
            "Gestión de lavado",
            "Soporte prioritario"
        ],
        cta: "Prueba 14 Días Gratis",
        highlighted: true,
        badge: "Más Popular"
    },
    {
        name: "Empresarial",
        price: "350.000",
        period: "/mes",
        description: "Para cadenas de parqueaderos con múltiples ubicaciones",
        features: [
            "Todo lo del Pro",
            "Sedes ilimitadas",
            "Dashboard consolidado multi-sede",
            "API para integraciones",
            "Usuarios ilimitados",
            "Roles y permisos personalizados",
            "Soporte 24/7 dedicado",
            "Capacitación incluida"
        ],
        cta: "Contactar Ventas",
        highlighted: false
    }
];
---

<section id="pricing" class="py-24 bg-gray-50">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="text-center max-w-3xl mx-auto mb-16">
            <h2 class="text-3xl md:text-5xl font-display font-bold text-gray-900 mb-6">Planes transparentes, sin sorpresas</h2>
            <p class="text-xl text-gray-600">Elige el plan perfecto para tu parqueadero. Todos incluyen 14 días de prueba gratis.</p>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-3 gap-8 mb-12">
            {plans.map((plan) => (
                <div class={`relative bg-white rounded-3xl shadow-lg border-2 transition-all hover:-translate-y-2 ${plan.highlighted ? 'border-brand-blue scale-105' : 'border-gray-100'}`}>
                    {plan.badge && (
                        <div class="absolute -top-4 left-1/2 -translate-x-1/2 bg-brand-yellow text-brand-blue px-4 py-1 rounded-full text-sm font-bold shadow-lg">
                            {plan.badge}
                        </div>
                    )}
                    
                    <div class="p-8">
                        <h3 class="text-2xl font-display font-bold text-gray-900 mb-2">{plan.name}</h3>
                        <p class="text-gray-600 text-sm mb-6">{plan.description}</p>
                        
                        <div class="mb-6">
                            <span class="text-4xl font-bold text-gray-900">${plan.price}</span>
                            <span class="text-gray-600">{plan.period}</span>
                        </div>

                        <a 
                            href="http://localhost:5173" 
                            class={`block w-full text-center py-3 rounded-xl font-bold transition-all mb-8 ${plan.highlighted ? 'bg-brand-blue text-white hover:bg-blue-900 shadow-lg' : 'bg-gray-100 text-gray-900 hover:bg-gray-200'}`}
                        >
                            {plan.cta}
                        </a>

                        <ul class="space-y-3">
                            {plan.features.map((feature) => (
                                <li class="flex items-start gap-3">
                                    <Check className={`w-5 h-5 flex-shrink-0 mt-0.5 ${feature.includes('✨') ? 'text-brand-yellow' : 'text-brand-green'}`} />
                                    <span class={`text-gray-700 ${feature.includes('✨') ? 'font-semibold' : ''}`}>{feature}</span>
                                </li>
                            ))}
                        </ul>
                    </div>
                </div>
            ))}
        </div>

        <!-- Billing Integration Note -->
        <div class="bg-blue-50 border-2 border-blue-200 rounded-2xl p-6 max-w-4xl mx-auto">
            <div class="flex items-start gap-4">
                <Zap className="w-6 h-6 text-brand-blue flex-shrink-0 mt-1" />
                <div>
                    <h4 class="font-bold text-gray-900 mb-2">Facturación Electrónica DIAN</h4>
                    <p class="text-gray-700 text-sm mb-3">
                        El plan <strong>Pro</strong> y <strong>Empresarial</strong> incluyen integración con proveedores de facturación electrónica certificados por la DIAN (Alegra, Siigo, Facturador.co).
                    </p>
                    <p class="text-gray-600 text-xs">
                        <strong>Nota:</strong> El servicio de facturación electrónica se contrata y paga directamente con el proveedor (~$50.000 - $150.000 COP/mes adicionales). Aparca solo cobra por la integración.
                    </p>
                </div>
            </div>
        </div>
    </div>
</section>
