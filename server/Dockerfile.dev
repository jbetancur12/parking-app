FROM node:20-alpine

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install dependencies
RUN npm install

# Don't copy source code, we will mount it as volume

EXPOSE 3000

# Start command for development
CMD ["npm", "run", "dev"]
